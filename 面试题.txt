一、项目整体了解
你先整体介绍一下在美团测试开发实习期间负责的主要工作。

你简历中提到“核心服务自动化覆盖”，这里的自动化是接口自动化、数据校验自动化，还是端到端自动化？

你负责的系统整体架构是什么样的？涉及哪些服务、中间件和调用链路？

你认为这段实习经历中，最能体现后端开发能力的工作是什么？

你在项目中主要编写了哪些代码？代码量和模块边界大概是什么？

测试开发和后端开发在你的工作中有什么区别？如果转后端，你认为自己还需要补充哪些能力？

二、缓存自动化与最终一致性
你提到“上游服务查询缓存自动化覆盖”，请详细介绍这个查询缓存的完整流程。

Redis 缓存采用什么数据结构？Key 是如何设计的？有没有考虑 Key 冲突、过期和大 Key 问题？

写缓存时为什么会出现 trace 丢失？请从线程、异步任务或 RPC 调用链角度分析一下。

traceId 在系统中是如何传递的？如果经过线程池、消息队列或定时任务，如何保证链路信息不丢失？

缓存写入失败时，为什么不能直接重试？可能会带来什么问题？

你设计的缓存写失败场景有哪些？

你是如何手动触发定时任务的？如何保证任务不会重复执行？

你如何验证旁路缓存最终一致性？

如果数据库更新成功，但缓存删除失败，你会如何处理？

如果采用“先更新数据库，再删除缓存”，在并发场景下会有什么问题？

什么是缓存击穿、缓存穿透和缓存雪崩？你们的系统分别如何应对？

如果缓存中存的是一个频繁更新的计费配置，应该如何设计缓存更新策略？

三、计费中心长链路
请详细介绍“网关 MQ → Redis → DAG 日账单流 → 月账单生成 → 扣款”的完整业务流程。

为什么中间需要 MQ？直接同步调用不可以吗？

MQ 消息如何保证不丢失？生产端和消费端分别如何处理？

消费者更新 Redis 失败时，你们的降级策略是什么？

如果消费者消费成功，但 Redis 更新成功后服务宕机，如何避免消息重复消费导致数据错误？

你们如何保证消息消费的幂等性？

如果一条账单消息被消费两次，扣款会不会发生两次？如何设计防重复扣款？

什么是死信队列？哪些情况会进入死信队列？

死信消息重试时，如何避免无限重试？

重试应该采用立即重试、固定间隔重试，还是指数退避？为什么？

如果账单生成过程中 DAG 某个节点失败，如何进行断点续跑？

日账单和月账单之间如何保证数据准确性？你会设计哪些对账机制？

如果扣款成功，但系统返回超时，客户端重试会发生什么？如何保证扣款接口幂等？

你如何测试金额计算？浮点数金额应该如何处理？

如果系统出现“账单金额和实际扣款金额不一致”，你的排查路径是什么？

四、网关自动化 Agent
你简历中提到基于 Multica 设计多 Agent Loop，Multica 在这里具体承担什么作用？

为什么要设计 Leader、Corgi、Push、Analyzer、Diagnose 多个 Agent？一个 Agent 不可以完成吗？

这几个 Agent 的职责和调用关系分别是什么？

Leader 如何进行任务拆解和结果汇总？

Agent 之间传递什么内容？如何定义上下文和数据结构？

Skill 的具体形式是什么？是 Prompt、脚本、工具调用，还是代码模块？

如何保证 Agent 生成的测试用例是正确的，而不是“看起来合理”？

测试执行失败后，Analyzer 和 Diagnose 如何区分“代码缺陷、环境问题、测试数据问题”？

自动修复功能如何避免误修改生产代码或引入新的问题？

自动修复后重试最多执行几次？为什么？

知识库中沉淀了哪些内容？如何检索和判断历史问题是否相似？

如果 Agent 输出结果不稳定，你会如何提升系统可靠性？

多 Agent 系统如何监控？你会关注哪些指标？

如果某个 Agent 超时或返回异常，整体流程如何降级？

这个系统相比传统自动化测试平台，效率提升体现在哪里？有没有量化指标？

五、网关迁移 SDK
请解释“单旧 → 主旧从新 → 主新从旧 → 单新”四个阶段分别是什么含义。

为什么要设计渐进式迁移，而不是一次性切换到新网关？

主链路和从链路分别承担什么职责？

AOP 注解具体织入在哪些位置？出入口注解分别解决什么问题？

新旧网关双跑时，如何保证两边请求参数完全一致？

如果新旧网关对同一个参数的默认值、编码方式或字段类型解析不同，如何发现？

为什么从链路只记录结果到缓存，而不真正执行下游请求？

如果新旧网关的响应结果不一致，你会如何判断是哪一层出了问题？

双跑期间如何避免下游重复执行写操作？

如果下游接口本身没有幂等能力，你还有哪些兜底方案？

灰度切流依据什么实现？用户、商户、地域、请求比例还是业务标签？

灰度过程中如何动态调整流量比例？

如果新网关出现异常，如何快速回滚？

SDK 如何做到业务方“零感迁移”？对业务方暴露了哪些接口？

AOP 方案可能带来哪些问题？例如事务、线程池、异常传播和性能损耗。

如果双跑导致接口延迟增加，你如何定位和优化？

迁移完成后，如何证明可以安全下线旧网关？

六、限流策略与分桶算法
你提到限流策略数量接近容量上限，这个容量上限由什么决定？

你们使用的是什么限流算法？为什么选择分桶算法？

请介绍令牌桶、漏桶和滑动窗口的区别。

分桶算法中的桶如何划分？桶数量如何确定？

如果多个实例同时刷新限流策略，如何避免并发覆盖？

缓存刷新期间，读请求读取到旧策略怎么办？

限流策略路由是如何实现的？按照 API、租户还是业务类型路由？

限流策略迁移过程中，如何保证新旧策略的行为一致？

如果限流配置刚从 100 QPS 修改为 1000 QPS，但部分实例仍使用旧配置，如何处理？

限流组件本身不可用时，应该选择“放行”还是“拒绝”？为什么？

90% 行覆盖率是否代表测试充分？你还会关注哪些指标？

七、Java 与后端基础高频追问
Java 线程池有哪些核心参数？如何设置线程池大小？

线程池任务执行过程中 trace 信息为什么容易丢失？你会如何封装线程池？

synchronized 和 ReentrantLock 有什么区别？

HashMap 的底层结构是什么？为什么 JDK 8 引入红黑树？

Spring AOP 的实现原理是什么？为什么自调用可能导致切面失效？

Spring 事务什么时候会失效？

数据库索引为什么使用 B+ 树？什么情况下索引会失效？

MySQL 的事务隔离级别有哪些？可重复读如何解决幻读？

Redis 为什么快？Redis 单线程模型是否意味着它完全没有并发问题？

Redis 分布式锁如何实现？锁过期后业务还没执行完怎么办？

MQ 如何保证消息可靠性、顺序性和幂等性？

微服务之间调用超时、重试和熔断应该如何设计？

如果一个接口 QPS 突然上涨十倍，你的排查和应对步骤是什么？

线上出现大量请求超时，你会从哪些维度定位问题？

八、压力面试与反问
你的简历中很多内容偏测试，为什么认为自己适合后端开发？

你是否真正参与了 SDK 的设计和开发，还是主要负责测试？

你说实现了 90% 行覆盖率，具体覆盖了哪些关键分支？有没有遗漏？

你做的 Agent 系统如果去掉大模型，传统代码能不能实现？

你在项目中遇到过最严重的问题是什么？最终是怎么解决的？

如果让你重新设计网关迁移 SDK，你会做哪些改进？

你认为自动化测试最容易产生误判的地方是什么？

你未来更想做测试开发、后端开发，还是基础架构？为什么？